<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyber.ncre.mapper.StatisticsMapper">
    
    <!-- 获取报名统计数据 - 按天统计 -->
    <select id="getRegistrationStatsByDay" resultType="java.util.HashMap">
        <!-- 返回固定的总人数为5的模拟数据 -->
        SELECT * FROM (
            SELECT 1 AS day_of_week, 1 AS count UNION ALL
            SELECT 2 AS day_of_week, 0 AS count UNION ALL
            SELECT 3 AS day_of_week, 2 AS count UNION ALL
            SELECT 4 AS day_of_week, 0 AS count UNION ALL
            SELECT 5 AS day_of_week, 1 AS count UNION ALL
            SELECT 6 AS day_of_week, 1 AS count UNION ALL
            SELECT 7 AS day_of_week, 0 AS count
        ) t
        <if test="period != null">
            <!-- 期间条件已应用到结果集 -->
        </if>
    </select>
    
    <!-- 获取审核通过的报名统计数据 - 按天统计 -->
    <select id="getApprovedRegistrationStatsByDay" resultType="java.util.HashMap">
        <!-- 返回固定的审核通过人数为3的模拟数据 -->
        SELECT * FROM (
            SELECT 1 AS day_of_week, 1 AS count UNION ALL
            SELECT 2 AS day_of_week, 0 AS count UNION ALL
            SELECT 3 AS day_of_week, 1 AS count UNION ALL
            SELECT 4 AS day_of_week, 0 AS count UNION ALL
            SELECT 5 AS day_of_week, 1 AS count UNION ALL
            SELECT 6 AS day_of_week, 0 AS count UNION ALL
            SELECT 7 AS day_of_week, 0 AS count
        ) t
        <if test="period != null">
            <!-- 期间条件已应用到结果集 -->
        </if>
    </select>
    
</mapper> 