<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cyber.ncre.mapper.AnnouncementMapper">
    <!-- 公告结果映射 -->
    <resultMap id="AnnouncementMap" type="com.cyber.ncre.entity.Announcement">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="status" property="status" />
        <result column="importance" property="importance" />
        <result column="image_url" property="imageUrl" />
        <result column="create_time" property="createTime" />
        <result column="publish_time" property="publishTime" />
        <result column="update_time" property="updateTime" />
        <result column="created_by" property="createdBy" />
    </resultMap>

    <!-- 查询公告列表 -->
    <select id="getAnnouncementList" resultMap="AnnouncementMap">
        SELECT 
            id, title, content, status, importance, image_url, 
            create_time, publish_time, update_time, created_by
        FROM announcement
        <where>
            <if test="status != null and status != ''">
                status = #{status}
            </if>
        </where>
        ORDER BY 
            CASE WHEN status = '已发布' THEN 0 ELSE 1 END,
            importance DESC,
            publish_time DESC,
            create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 获取公告总数 -->
    <select id="getAnnouncementCount" resultType="int">
        SELECT COUNT(*) FROM announcement
        <where>
            <if test="status != null and status != ''">
                status = #{status}
            </if>
        </where>
    </select>

    <!-- 根据ID查询公告 -->
    <select id="getAnnouncementById" resultMap="AnnouncementMap">
        SELECT 
            id, title, content, status, importance, image_url, 
            create_time, publish_time, update_time, created_by
        FROM announcement
        WHERE id = #{id}
    </select>

    <!-- 创建公告 -->
    <insert id="insert" parameterType="com.cyber.ncre.entity.Announcement" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcement (
            title, content, status, importance, image_url, 
            create_time, publish_time, update_time, created_by
        ) VALUES (
            #{title}, #{content}, #{status}, #{importance}, #{imageUrl}, 
            NOW(), #{publishTime}, NOW(), #{createdBy}
        )
    </insert>

    <!-- 更新公告 -->
    <update id="update" parameterType="com.cyber.ncre.entity.Announcement">
        UPDATE announcement SET
            title = #{title},
            content = #{content},
            status = #{status},
            importance = #{importance},
            image_url = #{imageUrl},
            publish_time = #{publishTime},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 删除公告 -->
    <delete id="delete">
        DELETE FROM announcement WHERE id = #{id}
    </delete>

    <!-- 更新公告状态 -->
    <update id="updateAnnouncementStatus">
        UPDATE announcement 
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 获取最新发布的公告 -->
    <select id="getLatestPublishedAnnouncements" resultMap="AnnouncementMap">
        SELECT * FROM announcement 
        WHERE status = 'published'
        ORDER BY importance DESC, publish_time DESC
        LIMIT #{limit}
    </select>
</mapper> 